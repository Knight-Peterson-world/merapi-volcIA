# ============================================================
# Merapi Anomaly Detection — requirements.txt
# Détection d'anomalies volcanique : scraping + EDA + PatchCore/DINOv2
# Compatible Apple Silicon (M1/M2) via MPS
# ============================================================

# --- Web scraping ---
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.1.0           # parser HTML rapide pour BeautifulSoup

# --- Data manipulation ---
pandas>=2.1.0
numpy>=1.26.0

# --- Images ---
Pillow>=10.2.0
opencv-python-headless>=4.9.0  # sans GUI (serveur/colab-friendly)
scikit-image>=0.22.0            # SSIM, métriques image

# --- Configuration ---
pyyaml>=6.0.1

# --- Utilitaires ---
tqdm>=4.66.0          # barres de progression
python-dateutil>=2.9.0

# --- Visualisation ---
matplotlib>=3.8.0
seaborn>=0.13.0

# --- Application ---
streamlit>=1.35.0
plotly>=5.18.0
kaleido>=0.2.1        # export PNG/SVG depuis Plotly

# --- Logging structuré ---
loguru>=0.7.2

# ============================================================
# V1 — DINOv2 + PatchCore + SVM
# ============================================================
scikit-learn>=1.4.0   # SVM, AUC-PR, F1, StratifiedKFold
scipy>=1.12.0         # mannwhitneyu (permutation test)

# ============================================================
# Torch — inférence DINOv2 / PatchCore (pas de génération SD)
# ============================================================
torch>=2.4.0
torchvision>=0.19.0
open-clip-torch>=2.24.0   # features DINOv2 / CLIP
# transformers<5.0 obligatoire : transformers>=5 casse open-clip-torch (LazyModule bug)
transformers>=4.44.0,<5.0  # DINOv2 via AutoModel (attention_maps.py)
