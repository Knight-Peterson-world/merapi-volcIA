# ============================================================
# requirements-render.txt — dépendances pour Streamlit Community Cloud
# Détection d'anomalies volcanique uniquement (PatchCore, DINOv2, dashboards)
# Pas de Stable Diffusion / LoRA / diffusers / transformers
# ============================================================

# --- Web ---
streamlit>=1.35.0,<2.0
plotly>=5.18.0

# --- Data ---
pandas>=2.1.0
numpy>=1.26.0

# --- Images ---
Pillow>=10.2.0
opencv-python-headless>=4.9.0

# --- ML léger ---
scikit-learn>=1.4.0
scipy>=1.12.0

# --- Config / utils ---
pyyaml>=6.0.1
loguru>=0.7.2
python-dateutil>=2.9.0

# --- Scraping (certaines pages) ---
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.1.0

# --- Torch CPU uniquement (PatchCore + DINOv2 inférence) ---
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu
torchvision==0.18.1+cpu

# open-clip requis par DINOv2 feature extraction
open-clip-torch>=2.24.0
# transformers<5.0 : DINOv2 AutoModel (attention_maps.py) — transformers>=5 casse open-clip
transformers>=4.44.0,<5.0
